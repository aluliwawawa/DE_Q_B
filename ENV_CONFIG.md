# 微信云托管环境变量配置快速参考

## 📋 需要配置的环境变量

复制以下内容到微信云托管控制台的环境变量配置中：

```
PORT=80
NODE_ENV=production
DB_HOST=111.229.111.28
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Aa123456!
DB_NAME=de_Q
WECHAT_APPID=wxd6039ac76feb6554
WECHAT_APPSECRET=9d8f881bd9ff50f673291bf794610fa0
JWT_SECRET=2jvkoZe4+pFRa3nT5/ZZfe9/jstr4+O75pU6HuPbD6g=
```

## 🔍 如何找到环境变量配置位置

### 方法1：服务详情页面
1. 登录 [微信云托管控制台](https://console.cloud.tencent.com/tcb)
2. 选择环境（如：`express-irfm`）
3. 进入 **云托管** -> **服务管理**
4. 点击服务名称（如：`express-irfm-001`）
5. 在服务详情页面找到 **"环境变量"** 或 **"配置"** 标签

### 方法2：版本管理
1. 进入 **云托管** -> **版本管理**
2. 点击 **"新建版本"** 或编辑现有版本
3. 在版本配置中找到 **"环境变量"** 设置

### 方法3：服务配置
1. 进入 **云托管** -> **服务管理**
2. 点击服务名称
3. 查看 **"服务配置"** -> **"环境变量"**

## ⚠️ 重要提示

1. **不要包含注释**：配置时只添加 `KEY=value` 格式，不要包含 `#` 开头的注释行
2. **每行一个变量**：如果支持多行文本，每行一个变量
3. **配置后重启**：修改环境变量后，必须重启服务才能生效
4. **检查数据库IP**：确保 `DB_HOST` 是云数据库的公网 IP，不是 `localhost`
5. **数据库防火墙**：确保云数据库允许微信云托管的 IP 访问

## ✅ 验证配置

配置完成后，查看服务日志，应该看到：
- ✅ `数据库连接成功`（而不是 `ECONNREFUSED`）
- ✅ `服务器运行在 http://localhost:80`

如果仍然看到 `ECONNREFUSED ::1:3306`，说明环境变量未生效，需要：
1. 检查环境变量是否正确配置
2. 重启服务
3. 检查数据库防火墙设置

